---
const { class: className, ...props } = Astro.props;
---

<div {...props} class:list={["hamburger cursor-pointer md:hidden", className]}>
  <span class="line"></span>
  <span class="line"></span>
  <span class="line"></span>
</div>
